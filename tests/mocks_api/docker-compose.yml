version: '3.8'
services:
  httpbin:
    image: kennethreitz/httpbin
    container_name: httpbin
    ports:
      - "8080:80"
    
  netcat:
    build:
      context: netcat
      dockerfile: Containerfile
    container_name: netcat
    ports:
      - "9999:8000"


